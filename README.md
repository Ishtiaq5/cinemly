# Cinemly

A premium dark-mode movie discovery & watchlist UI.

- **Stack:** vanilla HTML + CSS + JavaScript (no frameworks, no CDN, no build step)
- **Features:** instant search, genre filters, trending leaderboard, localStorage watchlist, glassmorphism + glow aesthetic
- **Run:** open `index.html` in any modern browser
